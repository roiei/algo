
import time
from util.util_list import *
from util.util_tree import *
import heapq
import copy
import collections
import operator


class Solution:
    def minSwaps(self, grid: [[int]]) -> int:
        rows = len(grid)
        cols = len(grid[0])
        
        def validate():

        def dfs():

        
            
            
stime = time.time()
print(3 == Solution().minSwaps(grid = 
    [[0,0,1],
     [1,1,0],
     [1,0,0]]))


print('elapse time: {} sec'.format(time.time() - stime))